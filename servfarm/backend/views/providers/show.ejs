<h2 class="page-header"><%= provider.name %></h2>
<div class="btn-row">
  <a class="btn btn-primary" href="/providers/<%= provider.id %>/edit">Edit</a>
  <%- include('../partials/deleteForm',
              { url: `/providers/${provider.id}` }) %>
</div>
<% if (servicesByProvider.length > 0) { %>
  <h2 class="page-header">Services By Provider</h2>
  <%- include('../partials/bookGrid', { services: servicesByProvider }) %>
<% } %>